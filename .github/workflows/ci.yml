name: CI

# Continuous integration for IB Desk.
#
# Runs on every push and pull request with no branch filter. Four independent
# jobs prove the system works: web (lint, typecheck, build), service (migrate,
# seed, lint, typecheck, test against a real pgvector Postgres), e2e (run the
# API and the web app, then drive them with Playwright), and docker (build the
# service image as a deployability check).
#
# The hard gates use the local storage backend (STORAGE_BACKEND: local), so no
# Supabase secret is needed. They also run the extraction engine in replay mode
# (LLM_MODE: replay), so the cassette logic tests need no OpenAI key. The service
# and e2e jobs apply every migration in db/migrations in sorted order, so new
# migrations run automatically.
#
# A fifth job, live-eval, is secret-gated and manual only. It runs the label-free
# evaluation harness against live OpenAI models. It triggers on workflow_dispatch
# alone, never on push or pull_request, and is skipped cleanly when the
# OPENAI_API_KEY secret is absent, so forks and key-less repositories do not fail.
#
# Action major versions are pinned. The env values below are local CI values,
# not secrets. No real credentials are committed. The only secret used anywhere
# in this workflow is OPENAI_API_KEY, used solely by the manual live-eval job.

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  web:
    name: Web lint, typecheck, build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck shared types
        run: pnpm run shared:typecheck

      - name: Lint web
        run: pnpm run web:lint

      - name: Typecheck web
        run: pnpm run web:typecheck

      - name: Build web
        run: pnpm run web:build

  service:
    name: Service migrate, seed, lint, typecheck, test
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/ib_desk
      STORAGE_BACKEND: local
      # Run the extraction engine in replay mode. The cassette logic tests are
      # backed by recorded cassettes and need no OpenAI key. This is also the
      # default; it is set here to keep the secret-free property explicit.
      LLM_MODE: replay
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ib_desk
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d ib_desk"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "0.11.7"

      - name: Sync dependencies
        working-directory: services/api
        run: uv sync

      - name: Apply migrations
        working-directory: services/api
        shell: bash
        run: for f in $(ls ../../db/migrations/*.sql | sort); do psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f "$f"; done

      - name: Seed sample data
        working-directory: services/api
        run: uv run python -m app.seed

      - name: Ruff check
        working-directory: services/api
        run: uv run ruff check .

      - name: Ruff format check
        working-directory: services/api
        run: uv run ruff format --check .

      - name: Mypy
        working-directory: services/api
        run: uv run mypy app

      - name: Pytest
        working-directory: services/api
        run: uv run pytest -q

  e2e:
    name: End to end
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/ib_desk
      NEXT_PUBLIC_API_BASE_URL: http://localhost:8000
      E2E_BASE_URL: http://localhost:3000
      WEB_ORIGIN: http://localhost:3000
      STORAGE_BACKEND: local
      E2E_PDF_FIXTURE: ${{ github.workspace }}/apps/web/e2e/sample.pdf
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ib_desk
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d ib_desk"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "0.11.7"

      - name: Sync service dependencies
        working-directory: services/api
        run: uv sync

      - name: Apply migrations
        shell: bash
        run: for f in $(ls db/migrations/*.sql | sort); do psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f "$f"; done

      - name: Seed sample data
        working-directory: services/api
        run: uv run python -m app.seed

      - name: Generate PDF fixture for Playwright
        working-directory: services/api
        shell: bash
        run: uv run python -c "from tests.fixtures_gen import born_digital_pdf; open('../../apps/web/e2e/sample.pdf','wb').write(born_digital_pdf())"

      - name: Start API
        working-directory: services/api
        run: nohup uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 > /tmp/api.log 2>&1 &

      - name: Wait for API
        shell: bash
        run: |
          set -euo pipefail
          for i in $(seq 1 60); do
            if curl -sf http://localhost:8000/health | jq -e '.database == "connected"' >/dev/null 2>&1; then
              echo "API is up and reports connected."
              exit 0
            fi
            sleep 1
          done
          echo "API did not report connected within 60 seconds."
          echo "----- /tmp/api.log -----"
          cat /tmp/api.log || true
          exit 1

      - name: Build web
        run: pnpm run web:build

      - name: Start web
        run: nohup pnpm run web:start > /tmp/web.log 2>&1 &

      - name: Wait for web
        shell: bash
        run: |
          set -euo pipefail
          for i in $(seq 1 60); do
            code=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 || true)
            if [ "$code" = "200" ]; then
              echo "Web is up and returned HTTP 200."
              exit 0
            fi
            sleep 1
          done
          echo "Web did not return HTTP 200 within 60 seconds."
          echo "----- /tmp/web.log -----"
          cat /tmp/web.log || true
          exit 1

      - name: Install Playwright browser
        run: pnpm --filter @ib-desk/web exec playwright install --with-deps chromium

      - name: Run Playwright tests
        run: pnpm --filter @ib-desk/web exec playwright test

      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: apps/web/playwright-report

  docker:
    name: Docker build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build service image
        run: docker build -t ib-desk-api services/api

  live-eval:
    name: Live eval (manual, secret-gated)
    runs-on: ubuntu-latest
    # Manual only. This job is defined under workflow_dispatch and is skipped on
    # push and pull_request. The if guard skips it cleanly when the OPENAI_API_KEY
    # secret is absent, so forks and key-less repositories do not fail. Secrets
    # are not exposed to fork pull requests, and workflow_dispatch cannot be run
    # from a fork, so the live key never runs in an untrusted context.
    if: ${{ github.event_name == 'workflow_dispatch' && secrets.OPENAI_API_KEY != '' }}
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/ib_desk
      STORAGE_BACKEND: local
      LLM_MODE: live
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      OPENAI_MODEL_DISCOVERY: gpt-5.5
      OPENAI_MODEL_EXTRACTION: gpt-5.5
      OPENAI_MODEL_VERIFICATION: gpt-5.5
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ib_desk
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d ib_desk"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "0.11.7"

      - name: Sync dependencies
        working-directory: services/api
        run: uv sync

      - name: Apply migrations
        working-directory: services/api
        shell: bash
        run: for f in $(ls ../../db/migrations/*.sql | sort); do psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f "$f"; done

      - name: Run label-free eval harness
        working-directory: services/api
        run: uv run python ../../evals/run_eval.py
